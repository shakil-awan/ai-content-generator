# API Configuration
BASE_URL=http://localhost:8001

# For Frontend Testing (Get actual keys from backend team)
# Note: These are placeholder values - request real keys from backend team

# Authentication
# JWT tokens are provided via /api/v1/auth/login endpoint

# Test Credentials (Development Only)
TEST_EMAIL=complete_test@demo.com
TEST_PASSWORD=test123456

# API Documentation
SWAGGER_UI=http://localhost:8001/docs
REDOC_UI=http://localhost:8001/redoc

# Stripe (Frontend - Publishable Key Only)
# STRIPE_PUBLISHABLE_KEY=pk_test_... (get from backend team)

# Important Notes:
# 1. Never commit real API keys to version control
# 2. Backend team will provide all necessary credentials
# 3. Use JWT tokens from login response for authenticated requests
# 4. All tokens expire after 24 hours - implement refresh flow
